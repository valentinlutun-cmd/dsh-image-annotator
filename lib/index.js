/**
 * dsh-image-annotator — host half.
 *
 * The feature is pure browser (lightbox augmentation + drawing canvas), so the
 * host body is empty; it only keeps the bundle row addressable.
 */
/** Host plugin body — dsh-image-annotator behavior exists only in the browser entry. */
function apply() {}

export { apply };
